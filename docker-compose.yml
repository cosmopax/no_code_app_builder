services:
  api:
    build: ./services/api
    ports: ["8001:8001"]
    restart: unless-stopped
  web:
    build: ./services/web
    ports: ["3000:3000"]
    environment:
      - NEXT_PUBLIC_API_BASE=http://localhost:8001
    depends_on: [api]
